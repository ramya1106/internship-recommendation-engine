# -----------------------properties-------------------------------

#spring.application.name=recommendation_sys
#
## MySQL Database Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/{db_name}
#spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
#spring.datasource.username={user_name}
#spring.datasource.password={password}
#
## JPA / Hibernate
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true


# spring.sql.init.mode = always --> only uncomment for the first run

# -----------------------properties-------------------------------

# STEPS TO RUN THE APPLICATION

# 1. CREATE application-local.properties under resources
# 2. Paste the above properties with modified values
# 3. Add application-local.properties to gitignore
# 4. Run the application in terminal using the below command

# command for running -->
# mvn spring-boot:run "-Dspring-boot.run.profiles=local"

